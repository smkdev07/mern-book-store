import React from 'react';
import { shallow } from 'enzyme';

import OrdersScreen from './OrdersScreen';

describe('<OrdersScreen />', () => {
  it('renders the <OrdersScreen /> component', () => {});
});
