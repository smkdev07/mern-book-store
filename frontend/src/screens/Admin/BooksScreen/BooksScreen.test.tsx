import React from 'react';
import { shallow } from 'enzyme';

import BooksScreen from './BooksScreen';

describe('<BooksScreen />', () => {
  it('renders the <BooksScreen /> component', () => {});
});
