import React from 'react';
import { shallow } from 'enzyme';

import OrderScreen from './OrderScreen';

describe('<OrderScreen />', () => {
  it('renders the <OrderScreen /> component', () => {});
});
