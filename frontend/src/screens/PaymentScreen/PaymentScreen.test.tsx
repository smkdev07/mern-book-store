import React from 'react';
import { shallow } from 'enzyme';

import PaymentScreen from './PaymentScreen';

describe('<PaymentScreen />', () => {
  it('renders the <PaymentScreen /> component', () => {});
});
