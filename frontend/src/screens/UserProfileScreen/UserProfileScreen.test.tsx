import React from 'react';
import { shallow } from 'enzyme';

import UserProfileScreen from './UserProfileScreen';

describe('<UserProfileScreen />', () => {
  it('renders the <UserProfileScreen /> component', () => {});
});
