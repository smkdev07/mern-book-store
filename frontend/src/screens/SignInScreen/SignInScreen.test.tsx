import React from 'react';
import { shallow } from 'enzyme';

import SignInScreen from './SignInScreen';

describe('<SignInScreen />', () => {
  it('renders the <SignInScreen /> component', () => {});
});
