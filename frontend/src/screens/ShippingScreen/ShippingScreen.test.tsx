import React from 'react';
import { shallow } from 'enzyme';

import ShippingScreen from './ShippingScreen';

describe('<ShippingScreen />', () => {
  it('renders the <ShippingScreen /> component', () => {});
});
